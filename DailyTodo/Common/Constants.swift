import CoreGraphics

enum Constants {
    // 最小屏幕逻辑分辨率
    static var MIN_SCREEN_SIZE = CGSize(width: 375, height: 812)
    // 最小Widget逻辑分辨率
    static var MIN_WIDGET_SIZE = CGSize(width: 141, height: 141)
}

