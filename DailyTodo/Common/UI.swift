import SwiftUI

enum AppColor {
    static let primary = Color("Primary")
    static let featureColor = Color("FeatureColor")
    static let featureBackgroudColor = Color("FeatureBackgroudColor")
}

enum AppFont {
    static let primary = Color("Primary")
}

enum AppImage {
    static let PSpace = "PSpace"
}
